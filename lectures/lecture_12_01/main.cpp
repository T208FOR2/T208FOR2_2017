#include <iostream>
#include <string>

using namespace std;

int main()
{
    string ord, setning;
    unsigned int pos;
    bool word_appears = false, word_appears_multiple_times = false;

    cin >> ord;
    getline(cin, setning);
    setning = setning.substr(1,setning.length());




    return 0;
}

